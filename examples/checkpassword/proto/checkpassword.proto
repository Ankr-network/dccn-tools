syntax = "proto3";

package checkpassword;

import "public/message.proto" ;


service CheckPWD {
    rpc Check( request ) returns ( response ) {}
}

message request {
    string username = 1;
    string password = 2;
}

message response {
     rsp = 1;
}